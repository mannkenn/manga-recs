# Copy to configs/local.toml and customize for your machine/environment.
#
# Credentials never belong in here. Put them in .env (see .env.example) or the
# ambient AWS credential chain; this file is only for non-secret settings.

[paths]
# data_dir = "data"
# artifacts_dir = "artifacts"

[storage]
# --- AWS S3 ---
# bucket = "manga-recs-dev"
# endpoint_url = ""
# region = "us-east-1"
#
# --- Cloudflare R2 ---
# bucket = "manga-recs"
# endpoint_url = "https://<account-id>.r2.cloudflarestorage.com"
# region = "auto"
#
# --- MinIO (docker compose up minio) ---
# bucket = "manga-recs"
# endpoint_url = "http://localhost:9000"
# region = "us-east-1"

[api]
# graphql_url = "https://graphql.anilist.co"
# fuzzy_match_threshold = 70

[ingestion]
# rate_limit = 10
# popularity_min = 10000
# user_start_id = 1001
# user_end_id = 1500
# user_max_pages = 200
# user_per_page = 50

[mlflow]
# experiment_name = "manga_cosine_recommender_dev"

[recommendation]
# default_top_n = 5

[evaluation]
# min_user_interactions = 5
# test_fraction = 20
# k = 10
# random_seed = 42
