query Query($userId: Int, $type: MediaType, $page: Int, $perPage: Int) {
  Page(page: $page, perPage: $perPage) {
    mediaList(userId: $userId, type: $type) {
      userId
      status
      score
      progress
      mediaId
      createdAt
    }
    pageInfo {
      hasNextPage
    }
  }
} 